apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sourcer.fullname" . }}-secret
type: Opaque
stringData:
  OAUTH2_CLIENT_SECRET: {{ required "secrets.OAUTH2_CLIENT_SECRET is required (inject from CI/CD secret store)" .Values.secrets.OAUTH2_CLIENT_SECRET | quote }}
