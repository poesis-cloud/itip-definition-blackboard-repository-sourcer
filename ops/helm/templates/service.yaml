apiVersion: v1
kind: Service
metadata:
  name: {{ include "sourcer.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sourcer.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
