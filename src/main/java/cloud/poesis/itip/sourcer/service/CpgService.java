package cloud.poesis.itip.sourcer.service;

import org.springframework.stereotype.Service;

/** Owns the loaded CPG (call edges, dataflow). Stateful, in-memory, lazy-prime. */
@Service
public class CpgService {}
