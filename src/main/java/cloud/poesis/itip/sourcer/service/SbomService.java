package cloud.poesis.itip.sourcer.service;

import org.springframework.stereotype.Service;

/** Owns the SBOM (CycloneDX/SPDX components). Stateful, in-memory, lazy-prime. */
@Service
public class SbomService {}
