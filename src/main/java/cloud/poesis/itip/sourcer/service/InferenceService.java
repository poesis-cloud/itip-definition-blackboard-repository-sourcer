package cloud.poesis.itip.sourcer.service;

import org.springframework.stereotype.Service;

/** LLM completion (used by Statement KSs in LLM-driven mode). External-facing. */
@Service
public class InferenceService {}
