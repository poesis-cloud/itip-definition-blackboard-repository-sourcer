{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bb://itip/blackboard/CodeGraph/ImplementationEdge/v1",
  "title": "ImplementationEdgeContribution",
  "description": "An implementation edge between a concrete type/method and an interface/abstract member it implements. Pattern-C universal slot (§8bis Rule 2). Producers (Rule 1): `ScipJavaKS` (and other per-language ScipKSs); `JoernKS`.",
  "type": "object",
  "required": [
    "fromSymbol",
    "toSymbol",
    "producerKind"
  ],
  "properties": {
    "fromSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the source endpoint (must resolve to a Index.ScipSymbol (or other code-bearing slot) in this Bb)."
    },
    "toSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the target endpoint (must resolve to a Index.ScipSymbol (or other code-bearing slot) in this Bb)."
    },
    "kind": {
      "type": "string",
      "enum": [
        "implements",
        "overrides"
      ]
    },
    "producerKind": {
      "type": "string",
      "description": "Pattern-C discriminator (§8bis Rule 2).",
      "enum": ["scip", "joern", "other"]
    },
    "nativeAttributes": {
      "type": "object",
      "description": "Pattern-C escape hatch (§8bis Rule 2).",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
