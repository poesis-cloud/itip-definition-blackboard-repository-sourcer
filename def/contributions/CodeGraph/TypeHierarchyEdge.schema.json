{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bb://itip/blackboard/CodeGraph/TypeHierarchyEdge/v1",
  "title": "TypeHierarchyEdgeContribution",
  "description": "A subtype/supertype edge between two type symbols. Pattern-C universal slot (§8bis Rule 2). Producers (Rule 1): `ScipJavaKS` and other per-language ScipKSs; `JoernKS`.",
  "type": "object",
  "required": [
    "fromSymbol",
    "toSymbol",
    "producerKind"
  ],
  "properties": {
    "fromSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the source endpoint (must resolve to a Index.ScipSymbol (or other code-bearing slot) in this Bb)."
    },
    "toSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the target endpoint (must resolve to a Index.ScipSymbol (or other code-bearing slot) in this Bb)."
    },
    "kind": {
      "type": "string",
      "enum": [
        "extends"
      ],
      "description": "Always 'extends' for this slot."
    },
    "producerKind": {
      "type": "string",
      "description": "Pattern-C discriminator (§8bis Rule 2).",
      "enum": ["scip", "joern", "other"]
    },
    "nativeAttributes": {
      "type": "object",
      "description": "Pattern-C escape hatch (§8bis Rule 2).",
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
