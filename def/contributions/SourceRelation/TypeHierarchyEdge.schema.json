{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bb://itip/blackboard/SourceRelation/TypeHierarchyEdge/v1",
  "title": "TypeHierarchyEdgeContribution",
  "description": "A subtype/supertype edge between two type symbols.",
  "type": "object",
  "required": [
    "fromSymbol",
    "toSymbol"
  ],
  "properties": {
    "fromSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the source endpoint (must resolve to a SourceUnit.CodeUnit in this Bb)."
    },
    "toSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the target endpoint (must resolve to a SourceUnit.CodeUnit in this Bb)."
    },
    "kind": {
      "type": "string",
      "enum": [
        "extends"
      ],
      "description": "Always 'extends' for this slot."
    }
  },
  "additionalProperties": false
}
