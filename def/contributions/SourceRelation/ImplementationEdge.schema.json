{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bb://itip/blackboard/SourceRelation/ImplementationEdge/v1",
  "title": "ImplementationEdgeContribution",
  "description": "An implementation edge between a concrete type/method and an interface/abstract member it implements.",
  "type": "object",
  "required": [
    "fromSymbol",
    "toSymbol"
  ],
  "properties": {
    "fromSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the source endpoint (must resolve to a SourceUnit.CodeUnit in this Bb)."
    },
    "toSymbol": {
      "type": "string",
      "description": "SCIP symbol id of the target endpoint (must resolve to a SourceUnit.CodeUnit in this Bb)."
    },
    "kind": {
      "type": "string",
      "enum": [
        "implements",
        "overrides"
      ]
    }
  },
  "additionalProperties": false
}
