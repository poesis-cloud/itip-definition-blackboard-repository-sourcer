# Default environment variables for the sourcer (non-secret defaults).
NAMESPACE=sie
DEPLOY_ENV=preprod

SOURCER_CHART=./ops/helm

# Outbound endpoints (overridable per environment)
BLACKBOARD_BASE_URL=http://sie-definition-blackboard-manager.sie.svc.cluster.local:8080
OIDC_ISSUER_URI=http://localhost:9000/realms/sie
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
